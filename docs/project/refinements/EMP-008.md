# Refinement EMP-008 — coverage, JaCoCo i quality evidence

Task-ID: EMP-008
Status: READY
Stan-Refinementu: ACCEPTED
Implementation: NOT_STARTED
Implementation-Allowed: YES
Coverage-Evidence: NOT_MEASURED
Javadoc-Warning-Baseline: 42
JaCoCo-Version: 0.8.15
Zaakceptował: Radosław Piątek
Data-Akceptacji: 2026-08-07
Scope-Frozen: YES

## Cel

EMP-008 nie dodaje funkcji biznesowej ani endpointu. Ma wprowadzić mierzalny gate JaCoCo oraz ręczny review luk testowych, aby `./mvnw verify` i `make verify` odrzucały regresję coverage bez maskowania kodu biznesowego przez łatwe do pokrycia klasy techniczne.

Aktualny evidence jest rzeczywisty, lecz bez pomiaru JaCoCo: 60 testów unit, 22 integration, `clean verify` i `make verify` przeszły; DocLint ma 0 errors i 42 warnings. Nie wolno twierdzić, że obecne LINE lub BRANCH osiąga proponowane progi przed pierwszym raportem.

## Aktualna macierz evidence

| Produkcja / krytyczność | Unit evidence | Integration / HTTP / runtime evidence | Ocena |
|---|---|---|---|
| `coupon.domain` — wysoka | `CouponCodeTest`, `CountryCodeTest`, `UserIdTest` | `CreateCouponApiIT`, `CouponRedemptionApiIT`, `DatabaseMigrationIT` | wystarczające dla kontraktów; `Coupon` ma pośrednie evidence przez API/DB |
| `coupon.application.CreateCouponService` — wysoka | `CreateCouponServiceTest` | create HTTP, duplicate i concurrent create | wystarczające |
| `coupon.application.RedeemCouponService` — wysoka | `RedeemCouponServiceTest` z `InOrder` i brakiem raw IP w command | redemption HTTP 403/503, happy path i concurrency | wystarczające po remediation EMP-004 |
| `TransactionalCouponRedemptionService` — krytyczna transakcja | brak izolowanego unit testu o niskiej wartości | PostgreSQL redemption, rollback INSERT/UPDATE, other constraint, 100/10, same-user, last-slot, row lock | wystarczające przez silniejsze evidence integration |
| `coupon.adapters.persistence` — krytyczna trwałość | brak mockowych testów SQL o niskiej wartości | `DatabaseMigrationIT`, rollback i endpoint IT na PostgreSQL | wystarczające; nie zastępować H2/mockami |
| web / DTO / Problem Details — wysoka granica API | częściowo validation przez domain/application | `CreateCouponApiIT`, `CouponRedemptionApiIT`, `CouponRedemptionErrorApiIT` | istnieją happy/conflict/403/503/privacy; malformed JSON i unknown field nie mają oddzielnego jawnego evidence — luka wartościowa B |
| Flyway V1/V2 — krytyczna | nie dotyczy | `DatabaseMigrationIT` obejmuje schema, constraints i visible ASCII `userId` | wystarczające |
| Client IP / CIDR / parsers — security-critical | `ClientIpAddressTest`, `ServletClientIpResolverTest`, `PublicIpAddressPolicyTest` | profile/config validation | wystarczające; osobny test parsera dawałby głównie lepszą diagnostykę, nie nową funkcję |
| ipwho.is / body / redirect — security-critical | `IpWhoisGeoLocationResolverTest` na WireMock | brak publicznej sieci, startup profile testy | wystarczające |
| `GeolocationConfiguration` / holders — średnia | `GeolocationConfigurationTest` | Spring context startup failure | wystarczające; proste holders nie wymagają testów getterów |
| canonical OpenAPI / Swagger — kontrakt | nie dotyczy | `OpenApiDocumentationIT`, JAR i Docker smoke | wystarczające |
| concurrent create/redemption — krytyczna | nie dotyczy | EMP-003 i EMP-009 exact-count Testcontainers evidence | wystarczające |
| Docker runtime — operacyjna | nie dotyczy | `make verify`: health, `/openapi.yaml`, `/swagger-ui`, dynamiczny loopback | wystarczające |

Klasyfikacja luk: A (krytyczna) — brak; B (wartościowa) — jawne HTTP evidence malformed JSON i unknown field, jeśli pierwszy raport lub API review wykaże niepokrytą gałąź; C (niska wartość) — bootstrap, proste DTO/records i configuration holders bez zachowania; D (pokryte silniejszym integration evidence) — persistence i transactional redemption. EMP-004 remediation już dodało `UserId`, orchestrator, 403/503, rollback, same-user, last-slot i no-global-lock; nie są to obecne luki EMP-008.

## Proponowany model JaCoCo

Przyszła implementacja dodaje oficjalny stabilny **`org.jacoco:jacoco-maven-plugin:0.8.15`**. Nie wolno użyć snapshotu 0.8.16 ani aktualizować wersji bez rzeczywistej potrzeby i formalnego amendmentu.

- agent uruchamia się dla Surefire i Failsafe bez nadpisywania istniejącego `argLine` (`@{argLine}` lub równoważne bezpieczne łączenie);
- dane unit i integration są scalane przed `verify` oraz generują HTML i XML;
- `target/site/jacoco/index.html` i `target/site/jacoco/jacoco.xml` powstają podczas `verify`, lecz nie są commitowane ani eksportowane;
- JaCoCo `check` jest wiązany z `verify`; failure coverage powoduje `BUILD FAILURE` i przez to awarię `make verify`;
- nie ma automatycznego obniżania progów, suppressowania failure ani luźnej komendy jako jedynej bramki;
- opcjonalne `make coverage-report` może jedynie ułatwiać lokalny odczyt; obowiązkowym gate pozostaje Maven `verify`.

Aktualny POM używa standardowego Surefire z Spring Boot parentem oraz `maven-failsafe-plugin:3.5.5`, bez jawnego `forkCount`, `reuseForks` i `argLine`. Implementacja musi zachować tę konfigurację i potwierdzić raportem, że oba typy testów wpływają na coverage oraz że Testcontainers nie zostają zakłócone.

## Metryki, agregacja i exclusions

Zaakceptowane globalne minimum to **LINE >= 80%** oraz **BRANCH >= 70%**. To jest docelowy minimalny gate, nie deklaracja aktualnego wyniku. Pierwszy raport ustala wyłącznie pomiar; wynik poniżej progu wymaga review missed lines/branches i wartościowego testu albo jawnego amendmentu właściciela — nigdy cichego obniżenia, tolerancji lub suppressowania failure.

Zaakceptowany model to kombinacja: globalne LINE/BRANCH oraz jeden łączny gate dla logicznych grup coupon domain, coupon application, coupon persistence i Client IP / GeoIP security-sensitive logic: **LINE >= 75%**, **BRANCH >= 65%**. Pełne nazwy Java package mają zostać odczytane z `src/main/java` podczas implementation checkpointu. Jeżeli JaCoCo nie potrafi wyrazić tej grupy bez objęcia niewłaściwego kodu, implementacja ma się zatrzymać i zgłosić problem — bez obniżenia progu, exclusion albo bezwartościowego testu.

Na początku nie ma żadnych exclusions produkcyjnego kodu — dotyczy to także Spring Boot main, configuration, DTO, exceptions i records/value objects. Bezwzględnie zakazane są exclusions dla domain, application, transakcji redemption, persistence, exception mapping, Client IP, CIDR/trusted proxy, GeoIP, adaptera providera i logiki security/privacy. Ewentualny późniejszy czysto mechaniczny artefakt wymaga dokładnego patternu, uzasadnienia, dowodu braku business/security behavior, wpisu w decision log i ponownego coverage gate; exclusion nie może służyć osiągnięciu progu.

## Manual gap review i polityka testów

Closeout EMP-008 wymaga nie tylko `coverage > threshold`, ale manualnego przeglądu `top missed instructions`, `top missed branches`, exception/validation/transaction/security paths. Nowy test jest dozwolony wyłącznie dla istotnego zachowania, nowej gałęzi, invariantu, security/privacy albo realnej luki z raportu. Testy getterów, pustych konstruktorów i duplikaty istniejącego IT są zabronione.

Zachowany standard EMP-004/009: deterministyczność, brak `Thread.sleep` i publicznej sieci, PostgreSQL Testcontainers dla DB, WireMock dla providerów, exact assertions, privacy-safe diagnostics, cleanup i brak flaky rerun. PIT/mutation testing jest **OUT_OF_SCOPE**: mały serwis ma już kosztowne evidence integration/concurrency, a JaCoCo plus ręczny gap review są proporcjonalne do zadania rekrutacyjnego.

## Javadoc warning policy

DocLint errors pozostają zawsze 0. Zapisane 42 warnings przejrzano na podstawie outputu ostatniego `clean verify`:

| Kategoria | Liczba | Ocena |
|---|---:|---|
| A — realny brak kontraktu | 18 | `CouponRedemptionRepository` (14) i `CouponRedemptionTransaction` (4) nie opisują kompletnej semantyki, wyników ani parametrów; należy naprawić. |
| B — techniczny bootstrap | 3 | Domyślne konstruktory aplikacji, konfiguracji i handlera Springa; nie wymagają sztucznego Javadoc, chyba że wynik implementacyjnego review wskaże inną przyczynę. |
| C — mała wartość | 2 | Puste komentarze wyjątków bez publicznej semantyki; można pozostawić tylko po jawnym uzasadnieniu baseline. |
| D — wymagają decyzji per typ | 19 | Rekordy command/response, `UserId`, kontroler, adapter JDBC i konstruktory usług: część może wymagać znaczącego kontraktu, ale nie wolno dopisywać mechanicznych `@param`. |

EMP-008 ma usunąć wszystkie 18 A, przejrzeć indywidualnie wszystkie 19 D i naprawić D dotyczące publicznego kontraktu, semantyki, failure mode, invariantu lub security/privacy. Nie wolno dodawać mechanicznych opisów tylko po to, by wyzerować warning. Po remediation może zostać najwyżej **5** technicznych albo bezwartościowych warnings; każdy musi być sklasyfikowany, uzasadniony i wymieniony w closeout evidence. **new warnings = 0**, a DocLint errors zawsze = 0.

### Stan evidence

`Coverage-Evidence: NOT_MEASURED`, ponieważ JaCoCo nie zostało jeszcze wdrożone. Status `READY` oznacza wyłącznie accepted refinement i dozwoloną implementację; nie oznacza osiągniętego LINE, BRANCH ani warning budget.

### Blokujące

Brak.

## Acceptance criteria

| ID | Kryterium |
|---|---|
| EMP008-AC-01 | JaCoCo 0.8.15 jest częścią Maven `verify`. |
| EMP008-AC-02 | `verify` generuje HTML i XML report. |
| EMP008-AC-03 | Globalne LINE >=80% jest egzekwowane. |
| EMP008-AC-04 | Globalne BRANCH >=70% jest egzekwowane. |
| EMP008-AC-05 | Critical package LINE >=75% jest egzekwowane. |
| EMP008-AC-06 | Critical package BRANCH >=65% jest egzekwowane. |
| EMP008-AC-07 | Coverage zawiera Surefire i Failsafe. |
| EMP008-AC-08 | Surefire/Failsafe współpracują z JaCoCo `argLine`. |
| EMP008-AC-09 | Nie ma domyślnych exclusions. |
| EMP008-AC-10 | Domain/application/persistence/security code nie mogą być wyłączone. |
| EMP008-AC-11 | Każdy przyszły exclusion wymaga jawnego uzasadnienia. |
| EMP008-AC-12 | `target/site/jacoco` nie trafia do Git ani source exportu. |
| EMP008-AC-13 | Manualny missed branch/line review jest wykonany przed closeoutem. |
| EMP008-AC-14 | Nowe testy dowodzą behavior, nie tylko procentu. |
| EMP008-AC-15 | DocLint errors = 0. |
| EMP008-AC-16 | Wszystkie 18 kategorii A Javadoc zostaje naprawionych. |
| EMP008-AC-17 | Wszystkie 19 kategorii D zostaje rozstrzygniętych indywidualnie. |
| EMP008-AC-18 | Finalny justified warning budget <=5. |
| EMP008-AC-19 | New Javadoc warnings = 0. |
| EMP008-AC-20 | `make verify` failuje po coverage regression. |
| EMP008-AC-21 | `scripts/check_emp008.py` weryfikuje rzeczywistą konfigurację. |
| EMP008-AC-22 | PIT pozostaje OUT_OF_SCOPE. |
| EMP008-AC-23 | Repozytorium nie zawiera `CODEX_PROMPT.md`. |

## Zaakceptowane decyzje właściciela

1. **ACCEPTED:** global LINE >=80%, bez automatycznego obniżenia.
2. **ACCEPTED:** global BRANCH >=70%, bez tolerancji ani suppressowania failure.
3. **ACCEPTED:** jedna krytyczna grupa package-level LINE >=75% / BRANCH >=65%.
4. **ACCEPTED:** `org.jacoco:jacoco-maven-plugin:0.8.15`, bez snapshotu 0.8.16.
5. **ACCEPTED:** brak default exclusions oraz ścisłe governance ewentualnego wyjątku.
6. **ACCEPTED:** 18 A obligatoryjnie naprawić, 19 D rozstrzygnąć indywidualnie, finalny justified budget <=5 i `new warnings = 0`.
7. **ACCEPTED:** testy malformed JSON/unknown fields tylko po realnym raporcie/manual gap review, nie z góry.
8. **ACCEPTED:** PIT OUT_OF_SCOPE.

## Poza zakresem

- implementacja JaCoCo, POM, thresholds, raporty `target/site/jacoco` i implementacyjny checker `scripts/check_emp008.py` — to jest dopiero przyszły, zamrożony scope implementation;
- Java, testy Java, OpenAPI, Docker/Compose, nowa funkcja biznesowa, EMP-009;
- mechaniczna naprawa 42 warnings bez oceny ich znaczenia.
