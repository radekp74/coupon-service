# Refinement EMP-009 — deterministyczne concurrency evidence

Task-ID: EMP-009
Status: DONE_AND_VERIFIED
Stan-Refinementu: ACCEPTED
Implementation: DONE_AND_VERIFIED
Implementation-Allowed: YES
Evidence-State: COMPLETE
Scope-Frozen: YES
Review-Result: PASS
Zaakceptował: Radosław Piątek
Data-Akceptacji: 2026-08-07

## Cel i granice

EMP-009 nie dodaje funkcji biznesowej ani endpointu. Formalizuje standard i mapuje istniejące evidence współbieżności z EMP-003 oraz EMP-004. Nie zmienia ich właścicieli implementacji: create coupon należy do EMP-003, redemption do EMP-004, a EMP-009 będzie właścicielem standardu i ewentualnego closeoutu evidence.

Rollback INSERT/UPDATE oraz mapping constraintu innego niż `uq_coupon_redemptions_coupon_user` pozostają obowiązkowym evidence EMP-004. Mogą być cytowane jako dowód spójności, ale nie są podstawowym kryterium concurrency EMP-009.

EMP-009 zamknięto dopiero po `make emp009-check`, pełnym Maven/Testcontainers gate i `make verify` z izolowanym Docker smoke. Implementacja dotknęła wyłącznie trzech zamrożonych asercji w istniejącym evidence oraz checkera; `Evidence-State` jest `COMPLETE`.

## Zamrożony zakres przyszłej implementacji

Dozwolone są wyłącznie zmiany istniejących testów współbieżności: (1) wszystkie 90 konfliktów 100/10 muszą mieć `COUPON_EXHAUSTED`, a baza musi zawierać 10 unikalnych userId; (2) last-slot musi potwierdzić zapis dokładnie jednego z dwóch konkurujących userId; (3) należy dodać `scripts/check_emp009.py`, `make emp009-check` oraz jego integrację z `verify.sh` i `make verify`.

Zakazane są nowe scenariusze biznesowe, endpointy, kod produkcyjny, migracje, OpenAPI, JaCoCo, coverage thresholds, Javadoc remediation i wszelkie zmiany EMP-008. Nie wolno kopiować testów ani zmieniać kryteriów EMP-003/EMP-004.

### Blokujące

Brak. Właściciel zaakceptował ograniczony scope przy jawnym `Evidence-State: PARTIAL`; brakujące asercje są zaplanowaną implementacją, nie nierozstrzygniętą decyzją.

## Zamrożony standard deterministyczności

- realny PostgreSQL przez Testcontainers, bez H2 i publicznej sieci;
- brak `Thread.sleep`, automatycznego retry testu i `rerunFailingTestsCount`;
- bounded `ExecutorService`, wspólny `CountDownLatch`, `CyclicBarrier` albo `Phaser`, readiness barrier gdy jest potrzebny;
- jawne timeouty, odebranie każdego `Future` bez ignorowania `ExecutionException`, `shutdownNow` i `awaitTermination` w `finally`;
- exact assertions dla wszystkich odpowiedzi, zero unknown outcomes, końcowy stan DB i izolacja danych między rundami;
- losowe porty Testcontainers są dozwolone; losowe dane tylko gdy nie zmieniają oczekiwanego rezultatu i pozostają diagnostyczne;
- failure raportuje round, zagregowane created/conflict/exhausted/unknown, `current_uses` i count records; nie raportuje raw IP, pełnego userId, sekretów ani odpowiedzi GeoIP.

## Acceptance criteria

| ID | Kryterium |
|---|---|
| EMP009-AC-01 | Concurrent create daje dokładnie 1 × 201, pozostałe duplicate conflict i 1 rekord. |
| EMP009-AC-02 | Co najmniej 3 rundy 100 różnych userId / maxUses=10 dają dokładnie 10 sukcesów i 90 `COUPON_EXHAUSTED`. |
| EMP009-AC-03 | Po każdej rundzie 100/10 `current_uses=10`, records=10 i 10 unikalnych userId. |
| EMP009-AC-04 | Same-user: 20 równoległych requestów daje 1 sukces i 19 `COUPON_ALREADY_REDEEMED`. |
| EMP009-AC-05 | Same-user kończy się `current_uses=1`, records=1 i zero exhausted. |
| EMP009-AC-06 | Last-slot: 2 różne userId dają 1 sukces i 1 `COUPON_EXHAUSTED`. |
| EMP009-AC-07 | Last-slot zachowuje `current_uses=max_uses`, count=max_uses i zapisuje dokładnie jednego z dwóch nowych userów. |
| EMP009-AC-08 | Kontrolowany row lock coupon-A nie blokuje redemption coupon-B przed release latcha. |
| EMP009-AC-09 | Żaden test evidence nie używa `Thread.sleep`. |
| EMP009-AC-10 | Evidence używa PostgreSQL/Testcontainers, nie H2 ani publicznej sieci. |
| EMP009-AC-11 | Wszystkie taski/futures/executory są rozliczone, timeoutowane i sprzątane. |
| EMP009-AC-12 | `make verify` uruchamia całe evidence bez zewnętrznego GeoIP. |
| EMP009-AC-13 | Implementacyjny checker po akceptacji wskazuje klasy, metody, synchronizację i asercje, nie same deklaracje. |
| EMP009-AC-14 | Release history zapisuje exact outcomes i czas gate. |
| EMP009-AC-15 | Pełny gate po implementacji uruchamia checker i całe evidence przed closeoutem. |

## Mapa istniejącego evidence

| AC | Klasa i metoda | Synchronizacja / exact assertion / state | Owner | Status |
|---|---|---|---|---|
| AC-01 | `CreateCouponApiIT.concurrentCaseVariantsProduceExactlyOneCreatedCoupon` | 3 rundy, 24 taski, ready/start latch; 1 created, 23 conflict, unknown empty, 1 rekord | EMP-003 | PRESENT |
| AC-02 | `CouponRedemptionApiIT.concurrentUsersRespectExactCapacityInThreeRounds` | 3 rundy, 100 tasków, ready/start latch; 10 created, 90 odpowiedzi z publicznym `code=COUPON_EXHAUSTED`, zero other/unknown | EMP-004/009 | PRESENT |
| AC-03 | ta sama metoda | `current_uses=10`, records=10, `COUNT(DISTINCT user_id)=10` w każdej rundzie | EMP-004/009 | PRESENT |
| AC-04/05 | `CouponRedemptionApiIT.sameUserConcurrentRetriesProduceExactlyOneSuccessAndNineteenConflicts` | 20 tasków, latch; 1 created, 19 conflict wyłącznie `COUPON_ALREADY_REDEEMED`, uses/records=1 | EMP-004 | PRESENT |
| AC-06 | `CouponRedemptionApiIT.twoDifferentUsersCompeteForTheLastSlotWithExactOutcomes` | 2 taski, latch; 1 created, 1 conflict wyłącznie `COUPON_EXHAUSTED` | EMP-004 | PRESENT |
| AC-07 | ta sama metoda | uses=2, records=2, PostgreSQL potwierdza dokładnie jednego z `user-A`/`user-B` | EMP-004/009 | PRESENT |
| AC-08 | `CouponRedemptionApiIT.rowLockOnOneCouponDoesNotGloballySerializeAnotherCoupon` | `SELECT ... FOR UPDATE` na A, release latch; future B kończy 201 przed release | EMP-004 | PRESENT |
| AC-09–12 | `CreateCouponApiIT` i `CouponRedemptionApiIT` | Testcontainers, latch, timeouty, futures i cleanup w `finally`; test profile GeoIP stub | EMP-003/004 | PRESENT |
| AC-13–15 | `scripts/check_emp009.py`, `make verify`, release history | checker analizuje testy/asercje; pełny gate przeszedł | EMP-009 | PRESENT |

## Decyzja zakresowa

Historyczny wynik review był `EVIDENCE_PARTIAL`: brakowały dokładnie trzy minimalne asercje. Właściciel formalnie zaakceptował ten ograniczony zakres 2026-08-07. Asercje i checker przeszły pełny gate, więc wszystkie AC są teraz `PRESENT`; status DONE wynika z rzeczywistego evidence, nie z samej dokumentacji.

## Poza zakresem

- EMP-008, JaCoCo, progi coverage i warning budget;
- naprawa 42 ostrzeżeń Javadoc: DocLint nie ma błędów, a ocena warningów należy do refinementu EMP-008;
- zmiana API, OpenAPI, Docker, Maven, kodu produkcyjnego oraz rollback evidence EMP-004.

## Historia

Audyt EMP-008/EMP-009 ujawnił przedwczesny closeout EMP-004; remediation EMP-004 uzupełniła same-user, last-slot, row lock i rollback evidence. Draft zmapował rzeczywiste metody i dał rekomendację `REJECT` dla closeoutu bez zmian testów. Właściciel zaakceptował refinement przy stanie `PARTIAL` oraz minimalnym zakresie trzech asercji i checkera. Implementacja uzupełniła wyłącznie te luki; pełny Maven/Testcontainers i Docker gate potwierdził `Evidence-State: COMPLETE` bez zmiany historii EMP-004.
